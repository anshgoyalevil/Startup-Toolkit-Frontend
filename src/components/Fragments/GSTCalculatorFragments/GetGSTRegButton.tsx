import { Button, ButtonProps } from '@mantine/core';
import React from 'react'

function GetGSTRegButton(props: ButtonProps) {
    return <Button variant="default" color="gray" {...props} />;
}

export default GetGSTRegButton